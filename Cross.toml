[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update -q && apt-get install -yq protobuf-compiler",
]

[target.aarch64-unknown-linux-gnu]
pre-build = [
    "apt-get update -q && apt-get install -yq protobuf-compiler",
]
