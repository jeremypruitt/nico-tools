name: Project board automation

# Drives card status on project 1 (https://github.com/users/jeremypruitt/projects/1)
# in response to issue + PR lifecycle events. See docs/agents/issue-tracker.md
# §"Project board automation" for the full transition table and the
# `PROJECT_PAT` secret setup required to grant project scope.

on:
  issues:
    types: [opened, labeled, closed, reopened]
  pull_request:
    types: [opened, ready_for_review, converted_to_draft, closed, reopened]
  projects_v2_item:
    types: [edited]

permissions:
  contents: read
  issues: write
  pull-requests: read

env:
  PROJECT_ID: PVT_kwHOAAE0oM4BXFC0
  STATUS_FIELD_ID: PVTSSF_lAHOAAE0oM4BXFC0zhSUcro
  STATUS_BACKLOG: a18af1e3
  STATUS_READY: 0855fb61
  STATUS_IN_PROGRESS: 7c242164
  STATUS_VALIDATING: f5779989
  STATUS_DONE: a8a3d908
  SCORE_FIELD_ID: PVTF_lAHOAAE0oM4BXFC0zhSUczU
  PRIORITY_FIELD_ID: PVTSSF_lAHOAAE0oM4BXFC0zhSUcyc
  PRIORITY_OPT_CRIT: c3817455
  PRIORITY_OPT_TOP: c34e5106
  PRIORITY_OPT_HIGH: 695030ca
  PRIORITY_OPT_MED: 8401f72b
  PRIORITY_OPT_LOW: 77940e7e

jobs:
  sync:
    if: github.event_name != 'projects_v2_item'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
    steps:
      - name: Determine target status from event
        id: status
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_ACTION: ${{ github.event.action }}
          LABEL_NAME: ${{ github.event.label.name }}
          PR_DRAFT: ${{ github.event.pull_request.draft }}
          PR_MERGED: ${{ github.event.pull_request.merged }}
        run: |
          set -euo pipefail
          STATE=""
          case "$EVENT_NAME.$EVENT_ACTION" in
            issues.opened|issues.reopened)
              STATE="$STATUS_BACKLOG"
              ;;
            issues.labeled)
              if [[ "$LABEL_NAME" == "ready-for-agent" || "$LABEL_NAME" == "ready-for-human" ]]; then
                STATE="$STATUS_READY"
              fi
              ;;
            issues.closed)
              STATE="$STATUS_DONE"
              ;;
            pull_request.opened|pull_request.reopened)
              if [[ "$PR_DRAFT" == "true" ]]; then
                STATE="$STATUS_IN_PROGRESS"
              else
                STATE="$STATUS_VALIDATING"
              fi
              ;;
            pull_request.ready_for_review)
              STATE="$STATUS_VALIDATING"
              ;;
            pull_request.converted_to_draft)
              STATE="$STATUS_IN_PROGRESS"
              ;;
            pull_request.closed)
              STATE="$STATUS_DONE"
              ;;
          esac
          if [[ -z "$STATE" ]]; then
            echo "no-op for $EVENT_NAME.$EVENT_ACTION"
          fi
          echo "state=$STATE" >> "$GITHUB_OUTPUT"

      - name: Sync content + linked issues to status
        if: steps.status.outputs.state != ''
        env:
          STATE: ${{ steps.status.outputs.state }}
          EVENT_NAME: ${{ github.event_name }}
          EVENT_ACTION: ${{ github.event.action }}
          PR_MERGED: ${{ github.event.pull_request.merged }}
          ISSUE_NODE_ID: ${{ github.event.issue.node_id }}
          PR_NODE_ID: ${{ github.event.pull_request.node_id }}
        run: |
          set -euo pipefail

          # Build the list of node IDs to sync.
          declare -a CONTENT_IDS
          if [[ "$EVENT_NAME" == "issues" ]]; then
            CONTENT_IDS+=("$ISSUE_NODE_ID")
          else
            CONTENT_IDS+=("$PR_NODE_ID")

            # PR closed without merge → only update the PR card; leave linked issues alone.
            if [[ "$EVENT_NAME" == "pull_request" && "$EVENT_ACTION" == "closed" && "$PR_MERGED" != "true" ]]; then
              echo "PR closed without merge — skipping linked-issue updates"
            else
              # Resolve linked issues via closingIssuesReferences.
              while IFS= read -r issue_node; do
                [[ -n "$issue_node" ]] && CONTENT_IDS+=("$issue_node")
              done < <(gh api graphql \
                -f query='query($id: ID!) {
                  node(id: $id) {
                    ... on PullRequest {
                      closingIssuesReferences(first: 20) { nodes { id } }
                    }
                  }
                }' \
                -F id="$PR_NODE_ID" \
                --jq '.data.node.closingIssuesReferences.nodes[].id')
            fi
          fi

          for CID in "${CONTENT_IDS[@]}"; do
            # Add to project — idempotent: returns the existing item if already added.
            ITEM_ID=$(gh api graphql \
              -f query='mutation($project: ID!, $content: ID!) {
                addProjectV2ItemById(input: {projectId: $project, contentId: $content}) {
                  item { id }
                }
              }' \
              -F project="$PROJECT_ID" \
              -F content="$CID" \
              --jq '.data.addProjectV2ItemById.item.id')

            # Set status.
            gh api graphql \
              -f query='mutation($project: ID!, $item: ID!, $field: ID!, $value: String!) {
                updateProjectV2ItemFieldValue(input: {
                  projectId: $project,
                  itemId: $item,
                  fieldId: $field,
                  value: { singleSelectOptionId: $value }
                }) {
                  projectV2Item { id }
                }
              }' \
              -F project="$PROJECT_ID" \
              -F item="$ITEM_ID" \
              -F field="$STATUS_FIELD_ID" \
              -F value="$STATE" > /dev/null

            echo "synced $CID → $STATE"
          done

  priority:
    # Fires on Score field changes; derives band, sets Priority single-select +
    # band label. Manual edits to Priority/label stick until Score changes again.
    if: github.event_name == 'projects_v2_item' && github.event.action == 'edited'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
    steps:
      - name: Filter to Score field changes
        id: filter
        env:
          CHANGED_FIELD_ID: ${{ github.event.changes.field_value.field_node_id }}
        run: |
          set -euo pipefail
          if [[ "$CHANGED_FIELD_ID" != "$SCORE_FIELD_ID" ]]; then
            echo "field-change is not Score (was $CHANGED_FIELD_ID); skipping"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Read item, derive band, propagate
        if: steps.filter.outputs.skip == 'false'
        env:
          ITEM_NODE_ID: ${{ github.event.projects_v2_item.node_id }}
        run: |
          set -euo pipefail

          # Read current Score value + linked issue (if content is an Issue).
          PAYLOAD=$(gh api graphql \
            -f query='query($id: ID!, $field: ID!) {
              node(id: $id) {
                ... on ProjectV2Item {
                  content {
                    __typename
                    ... on Issue { number repository { nameWithOwner } }
                  }
                  fieldValueByName(name: "Score") {
                    ... on ProjectV2ItemFieldNumberValue { number }
                  }
                }
              }
            }' \
            -F id="$ITEM_NODE_ID" \
            -F field="$SCORE_FIELD_ID")

          TYPENAME=$(echo "$PAYLOAD" | jq -r '.data.node.content.__typename')
          if [[ "$TYPENAME" != "Issue" ]]; then
            echo "content is $TYPENAME (not Issue); skipping label propagation"
            exit 0
          fi

          ISSUE_NUMBER=$(echo "$PAYLOAD" | jq -r '.data.node.content.number')
          REPO=$(echo "$PAYLOAD" | jq -r '.data.node.content.repository.nameWithOwner')
          SCORE=$(echo "$PAYLOAD" | jq -r '.data.node.fieldValueByName.number // empty')

          # Score cleared → strip band label + clear Priority field.
          if [[ -z "$SCORE" ]]; then
            echo "Score cleared on #$ISSUE_NUMBER; removing band labels + Priority field"
            for LABEL in crit top high med low; do
              gh issue edit "$ISSUE_NUMBER" --repo "$REPO" --remove-label "$LABEL" 2>/dev/null || true
            done
            gh api graphql \
              -f query='mutation($project: ID!, $item: ID!, $field: ID!) {
                clearProjectV2ItemFieldValue(input: {projectId: $project, itemId: $item, fieldId: $field}) {
                  projectV2Item { id }
                }
              }' \
              -F project="$PROJECT_ID" \
              -F item="$ITEM_NODE_ID" \
              -F field="$PRIORITY_FIELD_ID" > /dev/null
            exit 0
          fi

          # Clamp + derive band.
          SCORE_INT=$(printf '%.0f' "$SCORE")
          if (( SCORE_INT > 100 )); then SCORE_INT=100; fi
          if (( SCORE_INT < 1 )); then SCORE_INT=1; fi

          if   (( SCORE_INT >= 50 )); then BAND="crit"; OPT="$PRIORITY_OPT_CRIT"
          elif (( SCORE_INT >= 25 )); then BAND="top";  OPT="$PRIORITY_OPT_TOP"
          elif (( SCORE_INT >= 13 )); then BAND="high"; OPT="$PRIORITY_OPT_HIGH"
          elif (( SCORE_INT >= 5  )); then BAND="med";  OPT="$PRIORITY_OPT_MED"
          else                            BAND="low";  OPT="$PRIORITY_OPT_LOW"
          fi

          echo "Score=$SCORE_INT → band=$BAND on #$ISSUE_NUMBER"

          # Update Priority single-select.
          gh api graphql \
            -f query='mutation($project: ID!, $item: ID!, $field: ID!, $value: String!) {
              updateProjectV2ItemFieldValue(input: {
                projectId: $project,
                itemId: $item,
                fieldId: $field,
                value: { singleSelectOptionId: $value }
              }) {
                projectV2Item { id }
              }
            }' \
            -F project="$PROJECT_ID" \
            -F item="$ITEM_NODE_ID" \
            -F field="$PRIORITY_FIELD_ID" \
            -F value="$OPT" > /dev/null

          # Add the band label, remove the others.
          REMOVE=""
          for L in crit top high med low; do
            if [[ "$L" != "$BAND" ]]; then
              REMOVE+="${REMOVE:+,}$L"
            fi
          done
          gh issue edit "$ISSUE_NUMBER" --repo "$REPO" --add-label "$BAND" --remove-label "$REMOVE"
